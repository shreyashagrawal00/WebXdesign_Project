# QueueSmart - Smart Appointment and Queue Management System

**QueueSmart** is a modern web application designed to eliminate long waiting lines for government and healthcare services. It allows users to book appointments instantly, track their live position in the queue, and save valuable time.

## 🚀 Key Features

*   **Smart Booking**: Instantly book spots for essential services (Aadhaar, PAN, Passport, Hospital OPD).
*   **Live Queue Tracking**: Real-time updates on your current token number and estimated wait time.
*   **Dynamic Dashboard**: User-friendly interface to manage bookings and view history.
*   **Admin Controls**: Tools for officials to manage queues and process applications efficiently.
*   **Premium UI**: A polished, responsive interface featuring a "Dark Mode" aesthetic with high-contrast visuals.

## 🛠️ Tech Stack

*   **Frontend**: React.js, Tailwind CSS (Vanilla CSS for custom styling)
*   **Backend**: Node.js, Express.js
*   **Database**: MongoDB (Simulation/Mock DB for demo purposes)
*   **Real-time**: Socket.io for live queue updates

## 📦 Installation & Setup

1.  **Clone the repository**
    ```bash
    git clone https://github.com/yourusername/queuesmart.git
    cd queuesmart
    ```

2.  **Install Dependencies**
    We have a root script to install everything at once:
    ```bash
    npm install
    # This installs dependencies for both frontend and backend
    ```

3.  **Start the Application**
    ```bash
    npm start
    ```
    *   The backend server runs on `http://localhost:5001`.
    *   The frontend is served statically by the backend or can be run separately for dev (`cd frontend && npm run dev`).

## 🚀 Deployment

This project is configured for easy deployment on **Render.com**.

1.  Push your code to GitHub.
2.  Create a new Web Service on Render.
3.  Connect your repository.
4.  Use the following settings:
    *   **Build Command**: `npm install && npm run build`
    *   **Start Command**: `npm start`
5.  Deploy!

*For a detailed step-by-step guide, please refer to* `deployment_guide.md` *included in this repository.*

## 📂 Project Structure

*   `frontend/`: React application logic and UI components.
*   `backend/`: Node.js API server and Socket.io logic.
*   `package.json`: Root configuration for streamlined build/start commands.

---
*Generated by Google Agent*
